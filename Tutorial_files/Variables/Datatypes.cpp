// This files will contain different variable types.

#include <iostream>
using namespace std;
int main(int argc, char const *argv[])
{

    /*
        syntax to declare variablles in C++:

        data_type variable_name = value;

        examples are below this comment.

    */
    int people = 10;           // This variable type holds integer values.

    float weight = 50.5;       // his variable type hold decimal values.

    char bat = 'b';            // This variable type holds single characters like 'a', 'b', 'c' and so on.
    
    double big = 4.5598349846; // This variable type is just like float but can stores data with more precision.
    
    bool is_male = true;       // This variable type holds a "True" or "False" value.

    return 0;
}

/*Data-types defines the type of data a variable can hold.

Now there are 3 data types;

1.Built-in:- we saw the examples of these above.
2.User-Defined:- 
        a. struct
        b. Union
        c. Enum
3.Derived
        a.Array
        b.Function
        c.Pointer

*/